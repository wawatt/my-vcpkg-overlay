tesseract-robotics provides CMake targets tesseract::*.

  find_package(tesseract-robotics CONFIG REQUIRED)
  target_link_libraries(main PRIVATE tesseract::environment)

Plugin DLLs are in bin/; put that directory on PATH at runtime.
